package com.vehiclesales.car_connect_system.model;

public class Car extends Vehicle {
    public Car(String model, double price, String fuelType) {
        super("Car", model, price, fuelType);
    }
}